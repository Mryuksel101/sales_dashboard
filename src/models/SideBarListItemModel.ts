class SideBarListItemModel {
    to: string;
    label: string;

    constructor(to: string, label: string) {
        this.to = to;
        this.label = label;
    }
}

export default SideBarListItemModel;